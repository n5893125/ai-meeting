# 🚀 AI Meeting - 快速部署指南

## ✅ 當前狀態

- ✅ 專案已成功構建
- ✅ 所有檔案已準備在 `dist` 資料夾中
- ✅ Cloudflare Dashboard 已打開
- ✅ `dist` 資料夾已在檔案總管中打開
- ✅ 部署指南頁面已在瀏覽器中打開

## 📝 接下來的 3 個簡單步驟

### 步驟 1️⃣：上傳到 Cloudflare Pages

1. 在瀏覽器中的 Cloudflare Dashboard:
   - 點擊 **Workers & Pages**
   - **Create application** → **Pages** → **Upload assets**

2. 設定專案：
   - 專案名稱：`ai-meeting`
   - 將 `dist` 資料夾中的所有檔案拖曳上傳
   - 點擊 **Deploy site**

### 步驟 2️⃣：設定 API Key（必須！）

⚠️ **這是最重要的步驟！**

1. 部署完成後，在專案中：
   - **Settings** → **Environment variables** → **Add variable**

2. 新增變數：
   ```
   變數名稱: GEMINI_API_KEY
   變數類型: Encrypted (加密)
   變數值: [您的 Gemini API Key]
   環境: Production ✓  Preview ✓  (兩者都要勾選)
   ```

3. 點擊 **Save**

### 步驟 3️⃣：重新部署

1. **Deployments** 頁面
2. 最新部署旁的 **...** → **Retry deployment**
3. 等待部署完成

## 🎉 完成！

您的 AI Meeting 應用現在已經上線了！

---

## 📚 重要連結

- **Cloudflare Dashboard**: https://dash.cloudflare.com/
- **取得 Gemini API Key**: https://aistudio.google.com/
- **詳細部署指南**: 打開 `deployment-guide.html` 查看完整教程

---

## ❓ 常見問題

**Q: 如何取得 Gemini API Key？**
A: 前往 https://aistudio.google.com/ → Get API key → 創建並複製

**Q: 為什麼要設定環境變數？**
A: 為了安全性，API Key 不應該放在前端代碼中。使用 Cloudflare 的加密環境變數可以安全地存儲 API Key。

**Q: Functions 會自動部署嗎？**
A: 是的！`functions/api/` 目錄中的所有文件會自動部署為 Cloudflare Pages Functions。

**Q: 可以使用自訂域名嗎？**
A: 可以！在 Cloudflare Dashboard 的專案設定中，選擇 **Custom domains** 即可新增。

---

## 📂 專案結構

```
ai-meeting-build/
├── dist/                    # 構建輸出（要上傳的檔案）
│   ├── index.html
│   └── assets/
├── functions/               # Cloudflare Functions（會自動部署）
│   └── api/
│       ├── chat-create.ts
│       ├── chat-message.ts
│       ├── generate-image.ts
│       └── generate-report.ts
├── components/              # React 組件
├── services/                # 服務層
└── deployment-guide.html    # 視覺化部署指南
```

---

## 🔐 安全性說明

- ✅ API Key 使用加密環境變數存儲
- ✅ 所有 API 呼叫都在後端完成
- ✅ 前端不會暴露 API Key
- ✅ Cloudflare 提供 DDoS 防護

---

## 🎯 後續步驟（可選）

1. **設定自訂域名**：讓您的應用使用自己的域名
2. **配置分析**：啟用 Cloudflare Web Analytics 追蹤使用情況
3. **設定通知**：當部署失敗時接收通知
4. **連接 Git**：自動部署每次 Git push

---

**準備好了嗎？立即開始部署！** 🚀

如有任何問題，請查看 `deployment-guide.html` 中的詳細教程。
